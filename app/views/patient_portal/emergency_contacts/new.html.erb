<% content_for :title, "Add Emergency Contact" %>

<div class="max-w-2xl mx-auto">
  <div class="mb-6">
    <h1 class="text-2xl font-bold">Add Emergency Contact</h1>
    <p class="text-base-content/60 text-sm">Add someone we can contact in case of an emergency.</p>
  </div>

  <div class="card bg-base-100 shadow">
    <div class="card-body">
      <%= render "form", contact: @contact %>
    </div>
  </div>
</div>
