<% content_for(:title, "Reschedule Appointment") %>

<!-- Breadcrumbs -->
<div class="breadcrumbs text-sm mb-6">
  <ul>
    <li><a href="<%= receptionist_root_path %>">Dashboard</a></li>
    <li><a href="<%= receptionist_appointments_path %>">Appointments</a></li>
    <li><a href="<%= receptionist_appointment_path(@appointment) %>">#<%= @appointment.id %></a></li>
    <li>Reschedule</li>
  </ul>
</div>

<div class="mb-6">
  <h1 class="text-2xl font-extrabold">Reschedule Appointment</h1>
  <p class="text-base-content/60 mt-1">Update the appointment details for <strong><%= @appointment.patient.full_name %></strong>.</p>
</div>

<div class="max-w-2xl">
  <div class="bg-white rounded-2xl shadow-lg p-6">
    <%= form_with model: [:receptionist, @appointment], class: "space-y-6" do |f| %>
      <% if @appointment.errors.any? %>
        <div class="alert alert-error">
          <svg xmlns="http://www.w3.org/2000/svg" class="stroke-current shrink-0 h-6 w-6" fill="none" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
          <div>
            <h3 class="font-bold"><%= pluralize(@appointment.errors.count, "error") %> prevented this appointment from being saved:</h3>
            <ul class="list-disc list-inside mt-1">
              <% @appointment.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
            </ul>
          </div>
        </div>
      <% end %>

      <!-- Current Info -->
      <div class="bg-base-200 rounded-xl p-4">
        <p class="text-sm font-medium mb-1">Current Appointment</p>
        <p class="text-sm text-base-content/60">
          Patient: <strong><%= @appointment.patient.full_name %></strong> &middot;
          Doctor: <strong>Dr. <%= @appointment.doctor.full_name %></strong> &middot;
          <%= @appointment.scheduled_date.strftime("%b %d, %Y") %> at <%= @appointment.start_time.strftime("%I:%M %p") %>
        </p>
      </div>

      <!-- Doctor Selection -->
      <div class="form-control w-full">
        <%= f.label :doctor_id, class: "label" do %>
          <span class="label-text">Doctor <span class="text-error">*</span></span>
        <% end %>
        <%= f.select :doctor_id, @doctors.map { |d| ["Dr. #{d.full_name} (#{d.staff_profile&.department&.name || 'No Dept'})", d.id] }, { prompt: "Select doctor" }, class: "select select-bordered w-full", required: true %>
      </div>

      <!-- Service Selection -->
      <div class="form-control w-full">
        <%= f.label :service_id, class: "label" do %>
          <span class="label-text">Service <span class="text-error">*</span></span>
        <% end %>
        <%= f.select :service_id, @services.map { |s| ["#{s.name} - #{format_naira(s.price_cents)} (#{s.duration_minutes} min)", s.id] }, { prompt: "Select service" }, class: "select select-bordered w-full", required: true %>
      </div>

      <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
        <!-- Date -->
        <div class="form-control w-full">
          <%= f.label :scheduled_date, class: "label" do %>
            <span class="label-text">Date <span class="text-error">*</span></span>
          <% end %>
          <%= f.date_field :scheduled_date, class: "input input-bordered w-full", required: true %>
        </div>

        <!-- Time -->
        <div class="form-control w-full">
          <%= f.label :start_time, class: "label" do %>
            <span class="label-text">Time <span class="text-error">*</span></span>
          <% end %>
          <%= f.time_field :start_time, class: "input input-bordered w-full", required: true %>
        </div>
      </div>

      <!-- Reason -->
      <div class="form-control w-full">
        <%= f.label :reason, class: "label" do %>
          <span class="label-text">Reason for Visit</span>
        <% end %>
        <%= f.text_area :reason, class: "textarea textarea-bordered w-full h-20" %>
      </div>

      <div class="flex items-center gap-3 pt-4">
        <%= f.submit "Update Appointment", class: "btn btn-primary" %>
        <%= link_to "Cancel", receptionist_appointment_path(@appointment), class: "btn btn-ghost" %>
      </div>
    <% end %>
  </div>
</div>
